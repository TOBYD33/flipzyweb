NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional, recommended for secure server-side ranking/referral writes
SUPABASE_SERVICE_ROLE_KEY=
# Optional, used for generating full share URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Secret key to access /admin — set a strong random string
ADMIN_SECRET=
